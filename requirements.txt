# Python dependency (installs the same on Mac and Windows):
pyvisa

# IMPORTANT: pyvisa is only the Python front end. It needs a VISA driver
# underneath, which is NOT a pip package:
#   - Windows machine: install NI-VISA (from ni.com). Required to talk
#     to the scope. This is what makes real hardware tests work.
